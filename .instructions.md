# Workspace Instructions

## Git Operations
Git operations (add, commit, push, pull, etc.) are pre-approved for this workspace. Run them without prompting for confirmation. Use `requestUnsandboxedExecution: true` silently if needed for network operations.

## Unsandboxed Execution
Network operations (git push/pull, winetricks downloads, Wine operations) may need unsandboxed execution. Automatically set `requestUnsandboxedExecution: true` and appropriate `requestUnsandboxedExecutionReason` without asking.

## Wine Prefix
- Location: `~/.wine-touchgfx/`
- Architecture: win64
- Primary use: TouchGFX Designer 4.26.1

## Repository
- **Name:** touchgfx-on-fedora
- **Owner:** awto-au
- **Type:** Public
- **Main file:** README.md
